namespace Thavyra.Contracts.Authorization;

/// <summary>
/// Counts all stored authorizations, including those which are expired or revoked.
/// </summary>
/// <returns><see cref="Count"/></returns>
public record Authorization_Count;